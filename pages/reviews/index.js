// pages/reviews/index.js
Page({

    /**
     * 页面的初始数据
     */
    data: {

    },

    /**
     * 生命周期函数--监听页面加载
     */
    onLoad(options) {
        console.log("reviews onLoad", options)
        const page = this

        // 根据restaurant_id获取对应的reviews数据
        wx.request({
            url: `http://localhost:3000/api/v1/restaurants/${options.id}`,
            success(res) {
                console.log("reviews index", res)
                // 将reviews存储到dataObject中
                page.setData({ reviews: res.data.reviews.reverse(), name: res.data.name })
            }
        })
    },

    /**
     * 生命周期函数--监听页面初次渲染完成
     */
    onReady() {

    },

    /**
     * 生命周期函数--监听页面显示
     */
    onShow() {

    },

    /**
     * 生命周期函数--监听页面隐藏
     */
    onHide() {

    },

    /**
     * 生命周期函数--监听页面卸载
     */
    onUnload() {

    },

    /**
     * 页面相关事件处理函数--监听用户下拉动作
     */
    onPullDownRefresh() {

    },

    /**
     * 页面上拉触底事件的处理函数
     */
    onReachBottom() {

    },

    /**
     * 用户点击右上角分享
     */
    onShareAppMessage() {

    }
})